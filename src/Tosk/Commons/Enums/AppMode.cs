namespace Tosk.Commons.Enums
{
    public enum AppMode
    {
        Demo,     // Represents the app running in demo mode
        Live      // Represents the app running in live/production mode
    }
}
